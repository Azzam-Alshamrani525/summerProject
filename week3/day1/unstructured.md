i want a little command line thing that lets me track books im reading. like you run it and it asks you stuff. features needed: add a book (title author pages genre status), mark a book as done reading, rate it 1-5 stars once finished, list all books, filter by status (want to read / reading / finished), maybe search by author too idk if that's overkill. status can be one of three things - want to read, reading, finished. also each book needs a genre like fiction nonfiction scifi biography etc, doesnt need to be limited list can be freetext. storage - just use a json file locally called books.json dont need a database. data for each book roughly looks like this: {"title": "Dune", "author": "Frank Herbert", "pages": 412, "genre": "scifi", "status": "finished", "rating": 5}  ratings only exist once status is finished otherwise null. oh also need input validation - pages should be a number, rating should be 1-5 only, status has to be one of the three allowed values dont let user type garbage. nice to have not required: sort by rating, export list to a text file, count total pages read across all finished books. tech - python, no external libraries needed really just json and maybe a simple menu loop with input(). should run with python booktracker.py like everything else we do. this is basically a side thing to practice reading/writing json and doing basic CRUD without a real db.